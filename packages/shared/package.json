{
  "name": "@pluralsight/shared",
  "module": "src/index.ts",
  "private": true,
  "sideEffects": false,
  "scripts": {
    "build": "echo \"'shared workspace is private and should be skipped.\" && exit 1",
    "lint:js": "eslint --max-warnings 0 \"./**/*.@(cjs|mjs|ts)\"",
    "lint:ts": "tsc --project tsconfig.json --noEmit",
    "test": "echo \"'pnpm test' should be run from root directory.\" && exit 1"
  },
  "devDependencies": {
    "eslint-plugin-react": "^7.32.2",
    "eslint-plugin-react-hooks": "^4.6.0"
  },
  "dependencies": {
    "tslib": "latest"
  },
  "packageManager": "pnpm@8.6.11"
}
