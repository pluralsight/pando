{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["tsconfig.base.json", "eslintrc.cjs"],
  "pipeline": {
    "build": {},
    "build:packages": {},
    "build:website": {},
    "lint:css": {
      "inputs": ["**/*.css", "**/*.scss"]
    },
    "lint:js": {},
    "lint:ts": {
      "inputs": ["**/*.ts", "**/*.tsx"]
    },
    "test": {
      "inputs": ["tests/**/*"]
    }
  }
}
