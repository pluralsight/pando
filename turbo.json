{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": ["packages/**/*", "!packages/sandbox/**/*"]
    },
    "test": {
      "inputs": [
        "packages/**/*.ts",
        "packages/**/*.tsx",
        "packages/**/*.test.ts",
        "packages/**/*.test.tsx"
      ]
    },
    "lint:css": {
      "inputs": ["**/*.css", "**/*.scss"]
    },
    "lint:js": {
      "inputs": ["**/*.js", "**/*.ts", "**/*.tsx"]
    },
    "lint:ts": {
      "inputs": ["**/*.ts", "**/*.tsx"]
    }
  }
}
