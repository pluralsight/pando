---
title: 'Color - action'
description: "Pando's action color palette"
hero:
  tagline: 'A palette representing actionable and interactive elements.'
---

import ColorSwatch, { colorSwatchStyle } from 'src/components/ColorSwatch.astro'
import { css } from 'styled-system/css'

## Text colors

Use with all instances of interactive element text

<ColorSwatch
  title="text-initial"
  description="Text color inside of interactive items"
  uses="Buttons (filled, outline). Button icons"
  colorTokenName="action.text.initial"
  style={css.raw({ bgColor: 'action.text.initial' })}
/>

<ColorSwatch
  title="text-100"
  description="Inactive but actionable items and icons"
  uses="Toggle (off). Tabs (inactive)"
  colorTokenName="action.text.100"
  style={css.raw({ bgColor: 'action.text.100' })}
/>

<ColorSwatch
  title="text-200"
  description="Actionable text on its own"
  uses="Buttons (text)"
  colorTokenName="action.text.200"
  style={css.raw({ bgColor: 'action.text.200' })}
/>

<ColorSwatch
  title="text-inverse"
  description="Text on high-contrast interactive elements"
  uses="Button icons (no-shape). Toggle (hover)"
  colorTokenName="action.text.inverse"
  style={css.raw({ bgColor: 'action.text.inverse' })}
/>

## Navigation colors

Use with all instances of navigation text.

<ColorSwatch
  title="navigation-intial"
  description="Default link text color."
  uses="Any text link navigating to another page"
  colorTokenName="action.navigation.initial"
  style={css.raw({ bgColor: 'action.navigation.initial' })}
/>

<ColorSwatch
  title="navigation-hover"
  description="Text link hover state color"
  uses="Hover state of navigation text links"
  colorTokenName="action.navigation.hover"
  style={css.raw({ bgColor: 'action.navigation.hover' })}
/>

<ColorSwatch
  title="navigation-visited"
  description="Text link visited state color"
  uses="Visited state of navigation text links"
  colorTokenName="action.navigation.visited"
  style={css.raw({ bgColor: 'action.navigation.visited' })}
/>

## Border colors

All borders of non-interactive interface items

<ColorSwatch
  title="border-initial"
  description="border color of interactive elements"
  uses="Buttons (outline), Text/select fields, Radio/Checkbox, Toggle"
  colorTokenName="action.border.initial"
  style={css.raw({ bgColor: 'action.border.initial' })}
/>

<ColorSwatch
  title="border-focus"
  description="Outline border for focus state of elements"
  uses="Focus outline of any element"
  colorTokenName="action.border.focus"
  style={css.raw({ bgColor: 'action.border.focus' })}
/>

## Background colors

All color fills of interactive elements like avatar, skeleton, or progress

<ColorSwatch
  title="background-initial"
  description="Backgrounds for primary interactive elements"
  uses="Hover state background for primary interactive elements"
  colorTokenName="action.bg.initial"
  style={css.raw({ bgColor: 'action.bg.initial' })}
/>

<ColorSwatch
  title="background-hover"
  description="Hover state background for primary interactive elements"
  uses="Buttons (hover)"
  colorTokenName="action.bg.hover"
  style={css.raw({ bgColor: 'action.bg.hover' })}
/>

<ColorSwatch
  title="background-active"
  description="Backgrounds for primary interactive elements"
  uses="Buttons (active)"
  colorTokenName="action.bg.active"
  style={css.raw({ bgColor: 'action.bg.active' })}
/>

<ColorSwatch
  title="background-100"
  description="Backgrounds for default interactive elements"
  uses="Interactive card backgrounds, accordion backgrounds"
  colorTokenName="action.bg.100.initial"
  style={css.raw({ bgColor: 'action.bg.100.initial' })}
/>

<ColorSwatch
  title="background-100-hover"
  description="Hover state background for default interactive elements"
  uses="Interactive cards (hover), accordions (hover)"
  colorTokenName="action.bg.100.hover"
  style={css.raw({ bgColor: 'action.bg.100.hover' })}
/>

:::caution[Caution]
background-100-active is currently being evaluated and is not recommended for use
:::

<ColorSwatch
  title="background-100-active"
  uses="Active state background for default interactive elements"
  colorTokenName="action.bg.100.active"
  style={css.raw({ bgColor: 'action.bg.100.active' })}
/>
