---
import {css} from '../../styled-system/css';
import {flex} from '../../styled-system/patterns';
interface Props {
  title: string;
  description: string;
  uses: string;
  color: string;
}

const {title, description, uses, color} = Astro.props;
---
<div class={flex({direction:"row"})}>
  <div class={css({bgColor: 'neutral.surface.initial', height: "8", width: "8"})}>hi</div>
  <div>
    <div class={flex({direction: 'column'})}>
      <div>{title}</div>
      <div>{description}</div>
      <div>{uses}</div>
    </div>  
  </div>
</div>